model:
  d_model: 64
  nhead: 2
  num_encoder_layers: 1
  num_decoder_layers: 1
  dim_feedforward: 128
  dropout: 0.1

training:
  epochs: 1
  batch_size: 32
  learning_rate: 0.001
  warmup_steps: 10
  seed: 42

data:
  src_lang: en
  tgt_lang: de
  max_length: 64
  data_dir: data/processed/
